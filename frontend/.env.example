# Direct API URL (use when not proxying via Vercel rewrites)
# Example: https://your-tunnel.example.com or http://192.168.1.15:8080
VITE_POOL_API_URL=

# Set in Vercel project env (build time) to proxy /api on your custom domain:
# POOL_API_ORIGIN=https://your-tunnel.example.com